import {createFileRoute} from '@tanstack/react-router';import {useQuery} from '@tanstack/react-query';import {useServerFn} from '@tanstack/react-start';import {AppShell} from '@/components/layout/AppShell';import {PageHeader,Panel,Tag} from '@/components/common/Primitives';import {listBattleversiaCharacters} from '@/lib/aether/battleversia.functions';
export const Route=createFileRoute('/_authenticated/battleversia/characters')({head:()=>({meta:[{title:'Characters — Battleversia'}]}),component:Page});
function Page(){const load=useServerFn(listBattleversiaCharacters);const {data=[],isLoading}=useQuery({queryKey:['battleversia','characters'],queryFn:()=>load({data:{}})});return <AppShell><div className="space-y-6"><PageHeader title="Characters" description="Anime, Marvel and DC characters ranked by the persistent Battleversia character engine." backFallback="/battleversia"/>{isLoading?<Panel>Loading characters…</Panel>:<div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{data.map((c:any)=><Panel key={c.id}><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold">{c.name}</h2><p className="text-xs text-muted-foreground">{c.slug}</p></div><Tag tone="neutral">{c.universe}</Tag></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><span className="text-xs text-muted-foreground">Power</span><div className="font-semibold">{c.power_score}</div></div><div><span className="text-xs text-muted-foreground">Value</span><div className="font-semibold">◈ {Number(c.price).toLocaleString()}</div></div></div><p className="mt-4 text-xs text-muted-foreground">{c.market_available?'Available in market':'Not currently available'}</p></Panel>)}</div>}</div></AppShell>}
