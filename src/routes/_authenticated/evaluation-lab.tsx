import { useEffect, useMemo, useState } from "react";
import { createFileRoute } from "@tanstack/react-router";
import { FlaskConical, Play, Plus, RefreshCw } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { AppShell } from "@/components/layout/AppShell";
import { getEvaluationOverview, getEvaluationRuns, getEvaluationTestCases, runEvaluationTestCase, saveEvaluationTestCase } from "@/lib/admin/phase-v-evaluation.functions";

const TARGETS = ["agents","orchestrator","research","verification","knowledge","reports","notifications","modules","battle-versia"] as const;
type Target = typeof TARGETS[number];
const pretty = (s: string) => s.replaceAll("-", " ").replace(/\b\w/g, (m) => m.toUpperCase());
const pct = (n: number) => `${Math.round(n * 100)}%`;
export const Route = createFileRoute("/_authenticated/evaluation-lab")({ head: () => ({ meta: [{ title: "Evaluation Lab — Aether" }, { name: "description", content: "Operational evaluation, regression and quality control for Aether." }] }), component: EvaluationLab });

function EvaluationLab() {
  const [section, setSection] = useState<"overview"|"runs"|"cases"|Target>("overview"); const [overview, setOverview] = useState<any>(null); const [runs, setRuns] = useState<any[]>([]); const [cases, setCases] = useState<any[]>([]); const [loading, setLoading] = useState(true); const [message, setMessage] = useState("");
  const [form, setForm] = useState({ name: "", target: "orchestrator" as Target, input: '{\n  "request": "Research and verify this claim"\n}', expected: '{\n  "requiredFields": ["intent", "workflow"]\n}' });
  const refresh = async () => { setLoading(true); try { const [o, r, c] = await Promise.all([getEvaluationOverview(), getEvaluationRuns({ limit: 200 }), getEvaluationTestCases()]); setOverview(o); setRuns(r); setCases(c); } catch (e) { setMessage(e instanceof Error ? e.message : "Could not load Evaluation Lab"); } finally { setLoading(false); } };
  useEffect(() => { void refresh(); }, []);
  const visibleRuns = useMemo(() => TARGETS.includes(section as Target) ? runs.filter((r) => r.target === section) : runs, [runs, section]);
  const createCase = async () => { try { await saveEvaluationTestCase({ name: form.name || `${pretty(form.target)} evaluation`, target: form.target, input: JSON.parse(form.input), expectedOutcome: JSON.parse(form.expected) }); setMessage("Test case saved."); setForm((v) => ({ ...v, name: "" })); await refresh(); } catch (e) { setMessage(e instanceof Error ? e.message : "Invalid JSON or save failed"); } };
  const runCase = async (id: string) => { setMessage("Evaluation running…"); try { const result = await runEvaluationTestCase({ testCaseId: id }); setMessage(result.status === "completed" ? `Completed with score ${pct(Number(result.score))}.` : "Evaluation failed. The failure is recorded in Runs."); await refresh(); } catch (e) { setMessage(e instanceof Error ? e.message : "Evaluation failed"); } };
  return <AppShell><div className="space-y-6"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><FlaskConical className="h-5 w-5"/><h1 className="text-2xl font-semibold tracking-tight">Evaluation Lab</h1></div><p className="mt-1 text-sm text-muted-foreground">Independent operational evaluation, regression and quality control for Aether.</p></div><Button variant="outline" size="sm" onClick={() => void refresh()} disabled={loading}><RefreshCw className={`mr-2 h-4 w-4 ${loading ? "animate-spin" : ""}`}/>Refresh</Button></div>
    <div className="flex gap-2 overflow-x-auto pb-1">{["overview","runs","cases",...TARGETS].map((key) => <button key={key} onClick={() => setSection(key as typeof section)} className={`whitespace-nowrap rounded-md border px-3 py-2 text-xs font-medium ${section===key?"border-foreground bg-secondary text-foreground":"border-border text-muted-foreground hover:bg-secondary/60"}`}>{pretty(key)}</button>)}</div>
    {message && <div className="rounded-lg border border-border bg-secondary/40 px-4 py-3 text-sm">{message}</div>}
    {section === "overview" && <Overview overview={overview} runs={runs} cases={cases}/>} {section === "runs" && <Runs runs={runs}/>} {section === "cases" && <Cases cases={cases} form={form} setForm={setForm} onCreate={createCase} onRun={runCase}/>} {TARGETS.includes(section as Target) && <TargetView target={section as Target} runs={visibleRuns} cases={cases.filter(c => c.target === section)} onRun={runCase}/>} 
  </div></AppShell>;
}
function Overview({overview,runs,cases}:{overview:any;runs:any[];cases:any[]}) { const m=overview?.metrics ?? {totalRuns:0,passRate:0,averageLatencyMs:0,failureRate:0,retryRate:0,approvalRate:0}; return <div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Runs",m.totalRuns],["Pass rate",pct(m.passRate)],["Avg latency",`${Math.round(m.averageLatencyMs)} ms`],["Test cases",cases.length]].map(([l,v])=><Card key={String(l)}><CardHeader className="pb-2"><CardTitle className="text-sm font-medium text-muted-foreground">{l}</CardTitle></CardHeader><CardContent><div className="text-2xl font-semibold">{v}</div></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Operational metrics</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-3"><Metric label="Failure rate" value={pct(m.failureRate)}/><Metric label="Retry rate" value={pct(m.retryRate)}/><Metric label="Approval rate" value={pct(m.approvalRate)}/></CardContent></Card><Card><CardHeader><CardTitle>Recent runs</CardTitle></CardHeader><CardContent><Runs runs={runs.slice(0,8)}/></CardContent></Card></div> }
function Metric({label,value}:{label:string;value:string}) { return <div className="rounded-lg border p-4"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 text-xl font-semibold">{value}</p></div> }
function Runs({runs}:{runs:any[]}) { return <div className="space-y-2">{runs.length===0?<p className="py-8 text-center text-sm text-muted-foreground">No evaluations recorded yet. Create a test case and run it.</p>:runs.map((r)=><div key={r.id} className="rounded-lg border p-3"><div className="flex flex-wrap items-center gap-2"><span className="font-medium">{pretty(r.target)}</span><Badge variant={r.status==="completed"?(r.passed?"default":"destructive"):"outline"}>{r.status}</Badge><span className="ml-auto text-xs text-muted-foreground">{new Date(r.created_at).toLocaleString()}</span></div><div className="mt-2 grid grid-cols-2 gap-2 text-xs text-muted-foreground sm:grid-cols-4"><span>Score: {pct(Number(r.score))}</span><span>Latency: {r.latency_ms ?? "—"} ms</span><span>Version: {r.version}</span><span>Env: {r.environment}</span></div>{r.errors?.length ? <p className="mt-2 text-xs text-destructive">{r.errors[0]?.message}</p>:null}</div>)}</div> }
function Cases({cases,onRun,form,setForm,onCreate}:{cases:any[];onRun:(id:string)=>void;form:any;setForm:any;onCreate:()=>void}) { return <div className="space-y-6"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Plus className="h-4 w-4"/>Create test case</CardTitle></CardHeader><CardContent className="space-y-4"><Input placeholder="Test case name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><select className="h-10 w-full rounded-md border bg-background px-3 text-sm" value={form.target} onChange={e=>setForm({...form,target:e.target.value})}>{TARGETS.map(t=><option key={t} value={t}>{pretty(t)}</option>)}</select><Textarea aria-label="Evaluation input JSON" className="min-h-32 font-mono text-xs" value={form.input} onChange={e=>setForm({...form,input:e.target.value})}/><Textarea aria-label="Expected outcome JSON" className="min-h-24 font-mono text-xs" value={form.expected} onChange={e=>setForm({...form,expected:e.target.value})}/><Button onClick={onCreate}>Save test case</Button></CardContent></Card><Card><CardHeader><CardTitle>Reusable test cases</CardTitle></CardHeader><CardContent className="space-y-2">{cases.length===0?<p className="py-8 text-center text-sm text-muted-foreground">No test cases yet.</p>:cases.map(c=><div key={c.id} className="flex items-center gap-3 rounded-lg border p-3"><div className="min-w-0 flex-1"><p className="font-medium">{c.name}</p><p className="text-xs text-muted-foreground">{pretty(c.target)}</p></div><Button size="sm" onClick={()=>void onRun(c.id)}><Play className="mr-2 h-3.5 w-3.5"/>Run</Button></div>)}</CardContent></Card></div> }
function TargetView({target,runs,cases,onRun}:{target:Target;runs:any[];cases:any[];onRun:(id:string)=>void}) { return <div className="space-y-6"><Card><CardHeader><CardTitle>{pretty(target)} evaluations</CardTitle></CardHeader><CardContent><p className="text-sm text-muted-foreground">Recorded executions for this target. The engine fails closed when a real runtime adapter is unavailable; simulations are never reported as successful evaluations.</p></CardContent></Card><Card><CardHeader><CardTitle>Test cases</CardTitle></CardHeader><CardContent className="space-y-2">{cases.length?cases.map(c=><div key={c.id} className="flex items-center gap-3 rounded-lg border p-3"><span className="flex-1 text-sm">{c.name}</span><Button size="sm" onClick={()=>void onRun(c.id)}><Play className="mr-2 h-3.5 w-3.5"/>Run</Button></div>):<p className="text-sm text-muted-foreground">No test cases for this target.</p>}</CardContent></Card><Card><CardHeader><CardTitle>Runs</CardTitle></CardHeader><CardContent><Runs runs={runs}/></CardContent></Card></div> }
