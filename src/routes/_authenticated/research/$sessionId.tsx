import { createFileRoute } from "@tanstack/react-router";
import { useQuery } from "@tanstack/react-query";
import { useServerFn } from "@tanstack/react-start";
import { Activity, AlertTriangle, ArrowLeft, CheckCircle2, Link2, Search } from "lucide-react";
import { AppShell } from "@/components/layout/AppShell";
import { PageHeader, Panel, Tag, EmptyState } from "@/components/common/Primitives";
import { getMyResearchSession } from "@/lib/aether/research.functions";

export const Route = createFileRoute("/_authenticated/research/$sessionId")({
  head: () => ({ meta: [{ title: "Research Session — Aether" }] }),
  component: Page,
});

function Page() {
  const { sessionId } = Route.useParams();
  const load = useServerFn(getMyResearchSession);
  const { data, isLoading, error } = useQuery({ queryKey: ["my-research-session", sessionId], queryFn: () => load({ data: { sessionId } }) });
  if (isLoading) return <AppShell><Panel>Loading research session…</Panel></AppShell>;
  if (error || !data) return <AppShell><EmptyState title="Research session not found" description="This session is unavailable to the current account or project." icon={<AlertTriangle className="h-5 w-5" />} /></AppShell>;
  const { session, sources, plans, comparisons } = data as any;
  return <AppShell><div className="space-y-6"><PageHeader title={session.query} description="Durable native research session reconstructed from persisted state." backFallback="/research" /><div className="grid gap-3 sm:grid-cols-4"><Panel><p className="text-[10px] uppercase tracking-wider text-muted-foreground">Status</p><div className="mt-2"><Tag tone={session.status === "completed" ? "success" : session.status === "failed" ? "danger" : "neutral"}>{session.status}</Tag></div></Panel><Panel><p className="text-[10px] uppercase tracking-wider text-muted-foreground">Sources</p><p className="mt-2 font-mono text-xl">{sources.length}</p></Panel><Panel><p className="text-[10px] uppercase tracking-wider text-muted-foreground">Domains</p><p className="mt-2 font-mono text-xl">{new Set(sources.map((s: any) => s.domain)).size}</p></Panel><Panel><p className="text-[10px] uppercase tracking-wider text-muted-foreground">Last event</p><p className="mt-2 text-xs">{session.last_event_at ? new Date(session.last_event_at).toLocaleString() : "—"}</p></Panel></div><Panel><h2 className="flex items-center gap-2 text-sm font-semibold"><Search className="h-4 w-4" />Research plan</h2><div className="mt-4 space-y-3">{plans.length ? plans.map((plan: any) => <div key={plan.id} className="rounded-md border border-border/60 p-3"><div className="flex items-center justify-between gap-3"><span className="text-xs font-medium">{plan.strategy}</span><Tag tone={plan.status === "completed" ? "success" : "neutral"}>{plan.status}</Tag></div><p className="mt-2 text-xs text-muted-foreground">Queries: {Array.isArray(plan.queries) ? plan.queries.length : 0} · Unmet requirements: {Array.isArray(plan.unmet_requirements) ? plan.unmet_requirements.length : 0}</p></div>) : <p className="text-sm text-muted-foreground">No persisted plan.</p>}</div></Panel><Panel><h2 className="flex items-center gap-2 text-sm font-semibold"><Link2 className="h-4 w-4" />Sources and provenance</h2><div className="mt-4 space-y-3">{sources.length ? sources.map((source: any) => <div key={source.id} className="rounded-md border border-border/60 p-3"><div className="flex flex-wrap items-center justify-between gap-2"><div className="min-w-0"><p className="truncate text-sm font-medium">{source.title ?? source.url}</p><p className="truncate text-xs text-muted-foreground">{source.domain} · {source.provider} · {source.change_state ?? "new"}</p></div><Tag tone={source.status === "retrieved" ? "success" : "danger"}>{source.status}</Tag></div><p className="mt-2 break-all text-[11px] text-muted-foreground">Requested: {source.url}</p><p className="break-all text-[11px] text-muted-foreground">Canonical: {source.canonical_url ?? source.url}</p><div className="mt-2 flex flex-wrap gap-3 text-[11px] text-muted-foreground"><span>Hash: {source.content_hash ? source.content_hash.slice(0, 16) : "—"}</span><span>Attempts: {source.retrieval_attempts ?? 1}</span><span>Quality: {source.quality_score ?? "—"}</span><span>Freshness: {source.stale_reason ?? "fresh"}</span></div></div>) : <p className="text-sm text-muted-foreground">No sources persisted.</p>}</div></Panel><Panel><h2 className="flex items-center gap-2 text-sm font-semibold"><CheckCircle2 className="h-4 w-4" />Controlled comparisons</h2><div className="mt-4 space-y-3">{comparisons.length ? comparisons.map((comparison: any) => <div key={comparison.id} className="rounded-md border border-border/60 p-3"><p className="text-sm font-medium">{comparison.subject}</p><p className="mt-1 text-xs text-muted-foreground">Missing evidence: {comparison.missing_evidence?.length ?? 0} · Uncertainty: {comparison.uncertainty?.length ?? 0}</p></div>) : <p className="text-sm text-muted-foreground">No comparison has been persisted for this session.</p>}</div></Panel><div className="flex items-center gap-2 text-xs text-muted-foreground"><Activity className="h-3.5 w-3.5" />This page is reconstructed from durable research state; leaving the browser does not terminate the underlying runtime task.</div></div></AppShell>;
}
