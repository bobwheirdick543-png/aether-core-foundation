import {createServerFn} from '@tanstack/react-start';import {requireSupabaseAuth} from '@/integrations/supabase/auth-middleware';import {supabaseAdmin} from '@/integrations/supabase/client.server';import {authorizeSecurityAction} from './security-runtime';import {authorizeAgentAction} from './agent-runtime';
const db=supabaseAdmin as any;
async function gate(uid:string,action:string,permission:string,id:string){const s=await authorizeSecurityAction({actorId:uid,agentKey:'module',action,resourceType:'battleversia',resourceId:id,context:{phase:'R'}});if(!s.allowed)throw new Response(s.reason,{status:s.requiresApproval?409:403});const a=await authorizeAgentAction({agentKey:'module',permission,action,actorId:uid,metadata:{resourceId:id,domain:'battleversia'}});if(!a.allowed)throw new Response(a.reason,{status:a.requiresApproval?409:403});}
export const advanceBattleversiaAuction=createServerFn({method:'POST',strict:false}).middleware([requireSupabaseAuth]).inputValidator((x:any)=>({auctionId:String(x?.auctionId??'')})).handler(async({context,data})=>{await gate(context.userId,'battleversia.auction.advance','modules.run',data.auctionId);const {data:row,error}=await db.rpc('bv_advance_auction',{p_auction_id:data.auctionId,p_owner_id:context.userId});if(error)throw new Response(error.message,{status:409});return row;});
export const settleBattleversiaAuction=createServerFn({method:'POST',strict:false}).middleware([requireSupabaseAuth]).inputValidator((x:any)=>({auctionId:String(x?.auctionId??'')})).handler(async({context,data})=>{await gate(context.userId,'battleversia.auction.settle','modules.run',data.auctionId);const {data:row,error}=await db.rpc('bv_settle_auction',{p_auction_id:data.auctionId,p_owner_id:context.userId});if(error)throw new Response(error.message,{status:409});return row;});
export const startBattleversiaTournament=createServerFn({method:'POST',strict:false}).middleware([requireSupabaseAuth]).inputValidator((x:any)=>({tournamentId:String(x?.tournamentId??'')})).handler(async({context,data})=>{await gate(context.userId,'battleversia.tournament.start','modules.run',data.tournamentId);const {data:row,error}=await db.rpc('bv_start_tournament',{p_tournament_id:data.tournamentId,p_owner_id:context.userId});if(error)throw new Response(error.message,{status:409});return row;});
export const startBattleversiaMatch=createServerFn({method:'POST',strict:false}).middleware([requireSupabaseAuth]).inputValidator((x:any)=>({matchId:String(x?.matchId??'')})).handler(async({context,data})=>{await gate(context.userId,'battleversia.match.start','modules.run',data.matchId);const {data:row,error}=await db.rpc('bv_start_match',{p_match_id:data.matchId,p_owner_id:context.userId});if(error)throw new Response(error.message,{status:409});return row;});
export const completeBattleversiaMatch=createServerFn({method:'POST',strict:false}).middleware([requireSupabaseAuth]).inputValidator((x:any)=>({matchId:String(x?.matchId??''),winnerId:String(x?.winnerId??'')})).handler(async({context,data})=>{await gate(context.userId,'battleversia.match.complete','modules.run',data.matchId);const {data:row,error}=await db.rpc('bv_complete_match',{p_match_id:data.matchId,p_owner_id:context.userId,p_winner_id:data.winnerId});if(error)throw new Response(error.message,{status:409});return row;});
